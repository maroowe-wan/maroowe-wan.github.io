Kind: captions
Language: en
foreign
today we will be learning about how to
create a sample pipeline or first
pipeline in Jenkins okay we'll be using
a small pipeline script which will
download my Maven sample project and do
the building okay so let's go ahead we
have come to the Jenkins UI page on the
left hand side you can see new item
click on that okay a pop-up came give
your pipeline project as something
pipeline demo click on Pipeline and
click ok
so it has created one pipeline job now
inside that description
you can paste anything
okay and these options you can see the
similar options which I showed you in
the first freestyle job creation same
discard odd bills do not allow
concurrent builds GitHub project build
is parameterized if you come down you'll
see the build triggers how do you want
to trigger this pipeline project build
after other projects I'll build build
periodically GitHub
so all these options are there if you
scroll down you'll see the pipeline here
you have two options either you can
write a pipeline script here or if you
already have a pipeline script in your
GitHub only then from there you can
trigger it okay so I am going to show
you how to write a sample pipeline
script so that which will clone this
repo and download it and do the
packaging of that project okay so the
pipeline script will start with first
pipeline word
pipe line okay you open the curly brace
and as you press enter it will close the
curly base automatically next word will
be agent you can give any you can give
um
instead of any you can give other agent
suppose if you want to run Linux box or
Windows box or if you have multiple box
you can give the label of that box it
will go to that box and it will run
whatever the steps you will mention here
okay the next word will be stages
stages open the curly brace enter it
will close it stages means now we are
going to write multiple stage now next
you will type the stage and give the
name to the stage like I wanted to check
out
check out SCM I wanted to check out my
SCM okay open the curly brace press
enter it will close it okay next will be
steps
here I have to write the word steps open
the curly brace and click enter okay now
we have given one structure to our
pipeline
now what steps I have to follow I want
to check out my source code into my
agent could be any agent okay and
download it clone it so for doing that I
have to write some scripts here which
git will take care and it will download
the script for that you do not have to
go to internet you just scroll down
you'll see pipeline syntax here click on
that
okay if you scroll down you'll see lot
of all the plugins which is present here
or which supports the pipeline
functionality you will see everything
here I wanted to check out or download
or clone my git repo so you will see the
options check out from Version Control
select this okay you will see the drop
down you can see the SCM is get right
now only get is plugin is installed
that's why drop down is coming as good
it is asking what is the GitHub
repository URL come to your project okay
go to here you can use https or SSH as I
showed you previously
let me select https okay I have pasted
it here for cloning that I want
credentials of GitHub so I have added
one credentials for GitHub which will
using that it will clone the this
wrapper okay GitHub Java the branch will
be Main
why main because here we have a main
branch you can see this is a main
instead of Master so I have typed main
okay rest all will be the default when
you click on button generate pipeline
script it will generate and give it to
you this is the script now you do not
have to worry about writing all these
things you just copy this and
paste it here
you just have to copy and paste it here
and your first stage is done okay next
stage you have to type here after stage
one and two so two closing brace after
that again you type stage okay give some
name to your stage like build okay
open the curly brace close the curly
brace type steps open close okay now
again I have to do some building here so
since I am going to connect to the
windows slave agent in the same box okay
so I have to give the Windows command
okay so now come back again to this
pipeline syntax location select here bat
Windows batch script if you are going to
execute in Linux then there should be
something called shell script
since I am going to do it in Bat Windows
batch script I'll select this and my
command mvn package so I am going to
generate a script for this also so my
script is generated I'll copy this and
paste in the steps okay as you can see
checkout and package so this is the
simplest script pipeline script which I
am going to trigger now let's see with
how it works okay I hope you understood
this till here okay click on Save
click on apply and save button so our
first pipeline script is ready let's
trigger on build now
I have clicked on build now
building is going on as you can see it
has selected the credentials GitHub Java
it is it has taken a git as a tool okay
and it has cloned my main branch you can
see right and once it has cloned my repo
then it is doing mvn package mvn package
next command it is doing
okay it is going on and building is
successful
okay
next how to see this you can see here
this is the you can see here this is the
checkout sem which is done four seconds
build is done in seven seconds and both
are green the if you want to see a good
view of this pipeline then what you can
do is you can install the blue ocean
right now I have not installed the blue
ocean plugin if you install that blue
ocean plugin it will it will show a very
diagram wise that how the pipeline flow
is working
but for now this was the sample and the
smallest pipeline uh to test or to
practice on the Jenkins
I hope you understood this uh thank you
for watching this video