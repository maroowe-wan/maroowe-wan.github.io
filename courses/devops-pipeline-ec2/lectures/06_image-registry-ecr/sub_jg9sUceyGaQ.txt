Kind: captions
Language: en
if you're just getting started using
docker with
aws probably one of the first things
that you want to do
is to build a docker image and publish
it in the aws
registry in this tutorial i will show
you how you can use gitlab ci
to build and publish a docker image to
the aws
container registry service
so let me begin by first showing you the
application that i have
it's pretty simple i have here the
docker file what i'm doing i'm using the
nginx web server and i'm just copying
some files that i have here stored in
the public folder
it's actually just the index.html and
i'm putting it inside
nginx web server public folder to call
it like that
and this is my entire docker file the
process is pretty much similar
now what we're trying to do in this
tutorial is to take this docker file
build a docker image inside gitlab ci
and publish it in the aws registry
elastic registry container so for that
i'm going to first head to aws
inside the aws management console all
you have to do is to locate the elastic
container registry
click here on get started in case you
don't have any existing repositories
and here part of the repository name
will have an auto-generated name
and then what i'll do here kind of give
the name of the application i'm going to
call it my website
i will also enable this tag immutability
and this will ensure that i cannot
push the same tag more than once just a
safe measure there
and also this scan on push this is an
additional security check
just to make sure that your container is
secure it should be
anyway part of your ci cd pipeline but
just an additional level of security
doesn't hurt i don't need any encryption
and i'm going to go ahead and create a
repository
so at least from the graphical interface
it has been
pretty easy to get is up and running
now let's get into how to create our
gitlab ci pipeline
so inside the project i have here the
pipeline definition file
i'm just going to create one simple job
let's call it
publish by the way if you're just
getting started with gitlab ci
and the syntax and whatever i'm doing
here
is not clear i have a tutorial that i
will link here in upper part of the
screen
introduction could to gitlab ci
pipelines and also into the video
description you will find my gitlab ci
course uh it tells how to build
continuous integration continuous
deployment pipelines with gitlab ci
uh make sure you check the video
description otherwise i'm going to
assume that you are already familiar
with
how to write basic gitlab ci pipelines
so this will be the publish job and i'm
going to have here the script
and inside the script practically what
we're interested
in is running docker build so
docker build and we're gonna create here
a tag
and the tag will look something like
this so we'll have
uh i'm gonna define here some variables
so first of all we have the docker
registry
and the docker registry is this address
and i'm just going to remove my website
and the slash so that will be
that will be our docker registry and the
reason why i'm doing this inside
variables
is i like to keep the commands
free from any configuration free from
any things that
might interfere with what we're doing so
for that reason always good
to have variables you can easily change
them and you don't have to worry about
replacing them all over the place
i'm going to define also a second
variable i'm going to call it
app name and the app name is my website
site this is the exact same name i have
used
in aws if there's a mismatch there
the things will not work so what we're
doing is we're specifying
the docker registry and this will be
the name of the application and
additionally
we want to use some versions we
don't want to have only a latest or
something like that
so i'm going to use a predefined gitlab
ci variable
the variable is called ci pipeline
iid and this is like an incremental
number
what we'll do is we'll start with like
one two three four and so
on so um you'll have this versions
for your docker images as well it's a
simple way otherwise you can use any
other variables that you prefer
and don't forget here the dot at the end
this will just tell docker to build
everything that is in the current folder
now the thing with aws is that
when we're going to the next step and
the next step is we have to log into
docker so it's going to be docker
login and additionally what we're doing
after docker login is docker push
docker push
and again what we are pushing
is exactly this so again
specifying the registry specifying the
application name
so for aws it works a bit different
than for normal registries in the sense
that we don't have an username and a
password that we can directly use
so the command looks more or less
something like this we are going to
specify
here a parameter called username
the username will be aws and
we are not specifying any passwords here
so
we are going to read the password from
the standard input
so we're gonna have something like
password
sdd in and this will avoid
having the password somewhere here in
most recent docker versions
if you try to specify the password from
the command this will not work anymore
it's considered insecure
so for that reason the password will
come somewhere from the standard input i
will show you in a second what i'm
talking about
and we're also specifying here the
docker registry so
practically we are logging into this
docker registry
now we don't have a password so we need
here another command
that will give us the password and one
common way of doing that
is using the aws cli aws cli
is a tool created by amazon and it
allows you to interact with various
services
including the elastic container registry
service that they offer
so they will be able to provide us with
the password
so if you look at the documentation we
can use this get
login password command and then
practically we can
type this password to docker login
and then we don't have to worry about
the password itself
now of course in order to use this we
need to have
aws cli installed we're going to get
to that in a second but let's write the
command so we have here aws
e c r get
login password
and we're gonna pipe this password to
docker so
this password from standard input it's
gonna
come from aws so this is how it's
supposed to work at this level
now we have two problems first of all
we need docker in order to
run commands like build or login or push
and at the same time we need the aws cli
in order to interact with the aws
service in order to get a password
and provide it for the docker login so
in this case
there are multiple ways how you can
handle it
the way i did in this case is to use
the aws cli docker image inside gitlab
ci
and to install docker you can do it the
other way
you can use the docker image install
python
install the package manager for python
and install
aws cli as a python tool regardless how
you do it you still have to
to install something or the third option
would be to create your own docker image
that contains everything
not gonna go into that so the following
will happen we'll have here an image
and the image will be amazon aws
cli now this will bring us the aws cli
in version 2.
this is the first aws cli version that
has an official docker image before this
there were no official docker images
there were
some very popular community created
images
but in this case i just want to use
something configured from aws
and this is why we have this image here
now there's
a particular thing about this image and
it has an entry point meaning that when
you
start this image it will automatically
start opening the aws cli
and this is something that conflicts a
bit with how gitlab ci works
so for that reason we need to do the
following we need to specify
an additional configuration so we're
going to break this image
into the following steps we're going to
have here an image i'm going to specify
another property it's called
name the same name as we had before
we're gonna also specify an entry point
practically what we're doing is we're
overriding the entry point you wanna
tell
look for this image there's no entry
points when you start it
you more or less start it like a regular
image in gitlab ci where we
can decide from the script block which
commands we are running
so that is the first step there
what i like to do generally is to have a
before script block
and in that before script i'm just gonna
log some
important information and i'm also gonna
install uh tools that are missing just
to separate it from the script block
where it's
the actual functionality that we have
with this job so for example what i like
is to
have the version of the tools that we
need
displayed here so that if something
doesn't work we can later go back
and fix it and know exactly which
version has changed the same goes with
docker
but as you remember we still need to
install docker so the aws cli image will
not have docker automatically installed
so for that reason we have to go and use
the
amazon linux extras tool to install
and still in terms of having docker up
and running
this will make sure that docker is
installed as a client
and this will put here the version of
but when you start to actually use
doctor to build this image
it will show you a connection error you
will not be able to locate
a running docker daemon a running
instance of
docker that you need to collect with you
can either
practically start from the scripts the
daemon or what's happening in a lot of
gitlab ci
pipelines is that you use a service now
service allows you to start
another image i'm going to show you here
service
and the image that we're going to start
is docker and we're going to use
d i n d so docker in docker this is
actually services here
so we are starting this this will be
available over the network so it's not
an image that is running somehow inside
this awscli image that we have here but
we can
use it to connect to it so in order to
actually establish that connection
we're going to go ahead and define
another variable and you have to be very
careful how you write this variable has
to be exactly like this docker
underscore host
and we're going to use the tcp protocol
i'm going to connect to docker on port
2375. the name
docker is simply coming from the name of
the service that we have started here
so this is just an alias allows us over
the network to connect
to something and in this case the docker
client that we're installing here will
be able to connect to the docker host
docker daemon that is running and
actually run commands such as docker
build
so this is a bit of complication when we
are actually using
aws ecr because you don't have
a username and a password you need to
get the password
and the way to do it is by using the aws
sli
for other containers this is not the way
to do it
so this is definitely a disadvantage
when it comes to
using aws for storing docker containers
just as a sign node it makes definitely
things easier if you already have a lot
of things
running in aws infrastructure you don't
have
a container registry that you want to
maintain on your own so that's
definitely a positive thing
at the same time as i've demonstrated in
our tutorial which i'll link here above
you can just as well use the gitlab ci
built-in container
but then of course you will have to
establish the connection from the aws
infrastructure when you're using
services that use docker just wanted to
point out that
the way that we are doing here is less
than ideal
but this is how we do at least this is
the solution that i found
in order to get this to run with the
container service from aws
when using any aws commands typically
you need to specify in which
region the services are registered so
for that reason we're gonna
go ahead and also define an additional
environment variable it's called aws
default region
i think also aws region will work just
as well
so in this case for the docker registry
i can see the region directly in the url
but if you're unsure in which region you
are you need to check that inside your
aws account
so at least from a pipeline standpoint
we
have everything up and running we still
to establish our credentials in order to
be able to work with the aws cli
so to get that running as well i'm going
to go here to security identity and
compliance i'm going to select
iam which is the identity and access
management tool
from aws i'm going to click here on
users and what you're going to do is
going to add
a new user and when we're adding this
user we're practically going to get
a secret practical username and a
password like a secret and that will
allow us to use the aws cli
i'm just going to call it ecr user the
name itself
doesn't really matter what you need to
make sure is that you enable here
programmatic access this will
show us something at the end
we're going to attach some policies so
i'm going to go here to attach existing
policies i'm going to search for
container
and i'm going to use this permission
registry power user
it's a pretty file for permission but
this will not allow the user for example
to
delete the repository itself it's
less powerful than full access and
for just pushing containers that's
definitely more than enough
i don't need any tags i'm gonna go ahead
and review this
everything looks good and now
we have created this user now we have
here the access key id and the secret
access key
definitely do not want to store them
inside our pipeline
in any ways these are credentials that
we are trying
to protect from getting stolen so
storing them in
in git or anything like that it's never
a good idea
so for that reason i'm gonna go inside
my gitlab ci project
i'm gonna go to settings ci cd
and right here in the middle you should
see something like variables
and there are two variables that we are
going to define and these variables must
a specific name so i'm going to click
here on add variable
the first one is aws access key id
and what will happen is that
i'm going to put here the value but
this values will be available for
the pipeline when the pipeline is
executed
these environment variables will be
automatically injected by gitlab ci
and the cli tool will know to look for
the specific names
so practically it will be automatically
authenticate you to aws without you
doing
anything else just by providing these
variables but for that reason
they need to have a very specific name
if they don't have that specific name
it's definitely not going to work
so i am basic i pasted here all the
secrets
i'm gonna add these two variables and
both of them are here so in the pipeline
let's go ahead and commit those changes
and take a look at how the pipeline is
running
so the entire execution took less than
two minutes i'm definitely very happy
with this time
considering that we also needed to
install docker
and if you go back to elastic containing
uh we can check now if this
image has now been pushed in this case i
have version
19 i had also tried out a few things
before on the repository
so this is the name of the image the
name of the application and the version
so it definitely worked without any
issues if you look at the code
um it's just the steps that are required
in order to get
everything up and running so i haven't
noticed
anything suspicious so everything worked
properly
practically where our job starts is here
we're listing the aws version
listing the docker version that we're
using we're building the docker image
and these are parts all of the
of the docker build step and then we're
getting the password we're logging
in we're getting here a warning that
will be stored unencrypted
in this running docker container but we
don't have to worry too much about it
because the container will be destroyed
as soon as this job finishes
so we still need to store it somewhere
and then finally login succeeded so
everything is fine we're then pushing to
this repository
and as you've seen everything is inside
there
so let's go ahead and quickly review
what we have built here
so our entire pipeline resolves around
three docker commands that we have to
execute so
we are running docker build to build
this image and to tag it
in a specific version after that we are
using docker login to login to the
docker registry that we plan to use
in this case we're using aws and because
for aws we don't have a username and a
password as we typically have with
other docker registries we have to use
the aws cli
in order to get that password and this
is where the entire complication
comes why do we need aws cli
why don't we need to install docker on
top of this
why do we need docker in docker as a
to get everything up and running and
once everything
is there this command will execute of
course we'll give the password
through the pipe to docker login we're
logging in
and then we're just simply pushing
thank you for sticking until the end i
hope this video was helpful
and if that was the case make sure to
give it a thumbs up
and to consider subscribing to this
channel if you have any questions or if
something doesn't work make sure that
you leave a comment
in the section below and i'll do my best
to help you in the video description you
will find the entire little
ci pipeline file that i have used and
maybe if something changes in the future
i will also be posting updates in the
video description
hope to see you at another tutorial bye