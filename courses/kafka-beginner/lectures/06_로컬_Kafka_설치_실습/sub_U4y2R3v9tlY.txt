Kind: captions
Language: en
after completing this lesson you will be
able to Define
Kafka describe some use cases for
Kafka describe the Kafka data
model describe Kafka
architecture list the types of messaging
systems explain the importance of
Brokers kfka is a high performance
realtime messaging system it is an open
source tool and is a part of Apache
projects the characteristics of C are
kovka is a distributed and partitioned
messaging system that is highly fault
tolerant and scalable it has been tested
to process and send millions of messages
per second to several receivers Apache
kovka was originally developed by
LinkedIn to handle their log files and
later handed over to the open- source
community in early
2011 it became a main Apache project in
October
2012 a stable Apache cofa version 0
8.2.0 was released in February 2015 and
a stable Apache kfka version
08.21 was released in May 2015 which is
the latest version kofin can be used for
various purposes in an organization such
as messaging service millions of
messages can be sent and received in
real time realtime stream processing
kovka can be used to process a
continuous stream of information in real
time and pass data to stream processing
systems such as
storm log
aggregation Kafka can be used to collect
physical log files from multiple systems
and store it in essential location such
as
hdfs commit log service Kafka can be
used as an external commit log for
distributed
systems event sourcing a time-ordered
sequence of events can be maintained
through Kafka Kafka can be used to
aggregate user activity data such as
clicks navigation and searches from
different websites of an organization
such users activities can be sent to a
real-time monitoring system and Hadoop
system for offline
processing an example is Illustrated in
the image the information from customer
facing portals are sent in real time to
the Kafka cluster the Kafka cluster
consists of one or more servers that
process the messages in parallel the
information is sent to a real-time
monitoring system to monitor the user
clicks navigation and searches the
information is also saved in a Hadoop
system for offline processing the Kafka
data model consists of messages and
topics messages represent information
such as lines in a log file a row of
stock market data or an error message
from a system messages are grouped into
categories called topics for example log
message and stock
message the processes that publish
messages into a topic in Kafka are known
producers the processes that receive the
messages from a topic in kfka are known
as consumers the processes or servers
within Kafka that process the messages
are known as Brokers a Kafka cluster
consists of a set of Brokers that
process the messages the image
illustrates the Kafka data model it
shows a Kafka cluster that consists of
three Brokers there are two producers
sending messages to the kofka cluster
and two consumers receiving the messages
from the cluster producer one creates
messages for topic one whereas producer
2 sends messages for topic two these
messages are processed by the three
brokers in parallel and sent to the
consumers consumer 1 is interested in
topic two so it receives the messages
for topic two similarly consumer 2 is
interested in topic one so it receives
the messages for topic one the brokers
in the kofka cluster handle the process
of receiving storing and forwarding the
messages to the interested
consumers a topic is a category of
messages in Kafka The Producers publish
the messages into topics and the
consumers read the messages from topics
a topic is divided into one or more
partitions a partition is also known as
a commit log each partition contains an
ordered set of messages each message is
identified by its offset in the
partition messages are added at one end
of the partition and consumed at the
other the image illustrates a topic
simple that is divided into two
partitions the rights are completed at
one end and the reads are completed at
the other it shows six messages in
Partition zero and five messages in
Partition one the offset of message one
in Partition Z is zero as it is the
first message the offset of message six
in Partition Zer is five the messages
are written in the order 1 2 3 4 5 and
six whereas they are read in the same
order as 1 2 3 4 5 and six the next
message in Partition Zer will be message
7 which will be written at offset 6 the
next message for partition one will be
message six which will be written at
offset five topics are divided into
partitions which are the unit of
parallelism in Kafka partitions allow
messages in a topic to be distributed to
multiple servers or Brokers so that the
messages in a topic can be processed in
parallel a topic can have any number of
partitions each partition should fit in
a single Kafka server the number of
partitions in a topic decide the
parallelism of the topic the image
illustrates two partitions of a topic
simple partition zero consists of six
messages whereas partition one consists
of five
messages partitions can be distributed
across the Kafka cluster each Kafka
server or broker may handle one or more
partitions a partition can be replicated
across several servers for fault
tolerance one server is marked as a
leader for the partition and the others
are marked as followers the leader
controls the read and write for the
partition whereas the followers
replicate the data if a leader fails one
of the followers automatically become
the leader
zookeeper is used for the leader
selection as explained in the previous
lesson the image illustrates the
partition of a topic simple here the
partition zero is assigned to server one
and partition one is assigned to server
two these servers process the messages
in parallel to increase
throughput the producer is the creator
of the message in Kafka producers place
the message to a particular topic and
decide what partition to place the
message into for example a producer May
place a message into partition zero of
topic simple another producer May place
a message into partition one of topic
simple topics should already exist
before a message is placed by the
producer messages are added at one end
of the Partition by Kafka the image
illustrates a producer that creates
three messages and sends them to
different topics and partitions in Kafka
message one is sent to partition zero of
topic test one message two is sent to
partition one of topic test one and
message three is sent to partition zero
of topic test
two the consumer is the receiver of the
message in Kafka each consumer belongs
to a consumer group a consumer group may
have one or more consumers the consumers
specify what topics they want to listen
to a message is sent to all the
consumers in a consumer group the
consumer groups are used to control the
messaging
system the image illustr B to three
consumer groups they are consumer Group
1 consumer group 2 and consumer group 3
the consumer group one consists of three
consumers called consumer 1 consumer 2
and consumer 3 the consumer group two
consists of two consumers called
consumer 4 and consumer 5 the consumer
group three consists of a single
consumer called consumer 6 Kafka
architecture consists of Brokers that
take messages from the producers and add
to a partition of a topic Brokers
provide the messages to the consumers
from the
partitions The Producers create the
messages and send them to a particular
topic and a partition of a Kafka cluster
a topic is divided into multiple
partitions the messages are added to the
partitions at one end and consumed in
the same Order each partition acts as a
message cue consumers are divided into
consumer groups each message is
delivered to one consumer in each
consumer group
zookeeper is used for coordination among
the Kafka Brokers the image illustrates
the Kafka architecture that consists of
two partitions called Partition one and
partition two the two producers are
sending messages to the two brokers in
the cluster the Brokers add the messages
to the partitions and the messages are
taken from the partitions in the same
order as
insertion the messages are sent to two
consumer
groups the image also illustrates that
the cof cluster or Brokers interact with
zookeeper for distributed
coordination kofka architecture supports
two types of messaging systems known as
publish subscribe and Q system the
publish subscribe system is also called
pubsub in this system one system
broadcasts the messages and the
consumers subscribe to receive the
messages each message is received by all
the subscribers so if there are 100
messages published each subscriber
receives all the 100 messages in the
same order that they are produced in the
Q system each message has to be consumed
by only one consumer if there are
multiple consumers each message is
consumed by any one of the available
consumers in the same order that they
are
received the image illustrates the
implementation of a q system consumer 1
consumer 2 and consumer 3 belong to the
same consumer group so out of the six
messages two messages are received by
consumer one two messages by consumer 2
and two messages by consumer 3 note that
the messages are received in the same
order that they are produced so consumer
1 receives message one consumer 2
receives message two and consumer 3
receives message three after this
consumer 1 receives message 4 consumer 2
receives message five and consumer three
receives message
six the image illustrates the
implementation of a published subscribed
system consumer 1 consumer 2 and
consumer 3 belong to three separate
consumer groups so all the six messages
are sent to all the three consumer
groups called consumer group one
consumer group 2 and consumer group 3
since there is only one consumer in
consumer group one it receives all the
six messages in the order 1 2 3 4 5 and
six similarly consumer 2 and consumer 3
also receive all the six messages in the
same
order Brokers are the kfka process
processes that process the messages in
Kafka each machine in the cluster can
run one broker the Brokers coordinate
among each other using zookeeper one
broker acts as a leader for a partition
and handles the delivery and persistence
whereas the others act as followers
Brokers receive the message from the
producer and send it to the consumer
groups Kafka guarantees the following
guarantee one messages sent by a
producer to a topic and a partition are
pended in the same order this ensures
that the messages produced earlier do
not get ahead of the messages produced
later the time order is maintained very
strictly guarantee two a consumer
instance gets the messages in the same
order as they are produced which means
that the messages are never out of order
if the messages are produced in the
order 1 2 3 4 5 6 they will be received
in the order 1 2 3 4 5 and 6 this is
important in messaging system
as the dependency is on the time order
of
messages guarantee three a topic with
replication factor in tolerates up to n
minus one server failures for example
when the replication factor is specified
as three there will be no loss of
messages even if two machines
fail LinkedIn or the website
www.linkedin.com is the largest network
of professionals and is the originator
of Kafka Kafka is used by LinkedIn to
manage streams of information some of
the uses of Kafka at LinkedIn are as
follows monitoring Kafka is used to
collect metrics from various systems and
to create monitoring
dashboards messaging kfka is used as
message cues for Content feeds and as
publish subscrib system for
searches analytics Kafka is used to
collect page views and clicks from
customer facing websites and to store
the information into a central Hadoop
based analytic
system a building block for distributed
applications kfka is used as a building
block for distributed applications and
for building distributed databases and
distributed log systems kfka uses the
primary backup method of replication in
primary backup method one machine or one
replica is called a leader and is chosen
as the primary the remaining machines or
replicas are chosen as the followers and
act as backups the lead propagates the
rights to the followers and waits until
the rights are completed on all the
replicas if a replica is down it is
skipped for the right however kopka will
write a copy to the machine once it is
back if the leader fails one of the
followers will be chosen as the new
leader this mechanism can tolerate n
minus one failures if the replication
factor is n which can be specified at
the topic level persistence means a
message can be delivered even if the
machine that handles the message fails
Kafka uses the Linux file system for
Persistence of messages persistence
ensures no messages are lost kofka
relies on the file system page cache for
fast reads and writs all the data is
immediately written to a file in a file
system so that they can be recovered
even if the machine
fails messages are grouped as message
sets for more efficient rights message
sets can be compressed to reduce Network
bandwidth a standardized binary message
format is used among producers Brokers
and consumers to minimize data
modification here is a quick recap of
what we have learned in this lesson
Kafka is a high performance realtime
system kfka can be used as an external
commit log for distributed
systems Kafka data model consists of
messages and
topics kofka architecture consists of
Brokers that take messages from the
producers and add to a partition of a
topic kfka architecture supports two
types of messaging system called publish
subscribe and Q
system Brokers are the Kafka processes
that process the messages in
Kafka hey want to become an expert in
Big Data then subscribe to the simply
learn Channel and click here to watch
more such videos to nerd up and get
certified in Big Data click here