---
lecture_no: 2
title: 핵심 개념 - Topic, Partition, Offset, Broker
lecture_type: theory
sources:
  - https://www.youtube.com/watch?v=kj9JH3ZdsBQ
  - https://www.youtube.com/watch?v=y9BStKvVzSs
  - https://www.youtube.com/watch?v=UHjSP7nxk7g
---

# 핵심 개념 - Topic, Partition, Offset, Broker

## 학습 목표
- Topic이 메시지를 분류하는 논리 단위이며 Partition으로 나뉜다는 것을 이해한다
- Offset이 파티션 내 메시지의 순서/위치를 가리키는 의미를 설명한다
- Broker가 데이터를 저장·제공하는 역할임을 파악하고 네 개념의 관계를 그린다

## 본문

### 왜 이 주제를 배우는가
1강에서 Kafka를 "중간에서 메시지를 받아 전달하는 우체국"으로 비유했습니다. 이제 그 우체국 안을 들여다볼 차례입니다. Kafka를 다룰 때 가장 자주 마주치는 네 단어가 **Topic, Partition, Offset, Broker**입니다. 이 네 가지의 관계만 머릿속에 그릴 수 있으면, 이후 프로듀서·컨슈머를 배우는 일이 훨씬 쉬워집니다.

### Topic - 메시지를 분류하는 논리 단위
**Topic(토픽)** 은 같은 종류의 이벤트를 모아 두는 이름표 붙은 통입니다. 우체국에 "편지 창구", "대형 택배 창구"가 따로 있듯이, Kafka도 `orders`(주문), `payments`(결제), `trucks_gps`(차량 위치)처럼 데이터 성격별로 토픽을 나눕니다. 토픽은 **이름으로 구분**되며, 토픽을 어떻게 나눌지는 데이터베이스 테이블을 설계하듯 개발자가 직접 정합니다.

토픽을 데이터베이스 테이블에 비유하면 이해가 쉽지만, 중요한 차이가 있습니다. 토픽은 테이블처럼 **조건으로 검색(query)할 수 없습니다.** 데이터를 뒤에 계속 덧붙이고(append), 앞에서부터 순서대로 읽기만 합니다.

더 정확한 비유는 **로그(log)** 입니다. 여기서 로그란 "일이 일어난 순서대로 한 줄씩 쌓이는 기록"을 뜻합니다. 토픽이 로그라는 사실은 세 가지 성질을 만듭니다.

- **추가 전용(append-only):** 새 메시지는 항상 맨 끝에 붙습니다. 중간에 끼워 넣지 않습니다.
- **불변(immutable):** 한 번 기록된 이벤트는 컨슈머가 임의로 수정하거나 지울 수 없습니다. 이미 일어난 일을 되돌릴 수 없는 것과 같습니다. (보관 기간이 지나면 오래된 메시지가 통째로 정리되긴 하지만, 개별 메시지를 골라 고치는 일은 없습니다.)
- **보관 가능(durable):** 디스크 파일로 저장되며, 설정한 보관 기간 동안 남아 있습니다. 기본값은 보통 일주일이지만 몇 초로도, 사실상 영구로도 설정할 수 있습니다.

### Partition - Topic을 나눠 확장한다
토픽 하나에 모든 메시지를 한 줄로 쌓으면, 데이터가 많아질 때 한 서버가 모두 감당해야 해서 병목이 생깁니다. 그래서 Kafka는 토픽을 **Partition(파티션)** 이라는 여러 조각으로 나눕니다.

우체국 비유로 돌아가면, "편지 창구"가 너무 붐빌 때 직원을 여러 명 두고 유럽행·미국행·아시아행으로 나눠 처리하는 것과 같습니다. 파티션이 여러 개면 메시지를 여러 곳에 동시에 쓰고 읽을 수 있어, **병렬 처리와 높은 처리량(throughput)** 이 가능해집니다. 파티션은 서로 다른 서버에 흩어져 놓일 수 있고, 이것이 Kafka가 수평으로 확장(scale-out)되는 비결입니다.

예를 들어 `trucks_gps` 토픽을 12개 파티션으로 만들면, 수많은 차량의 위치 데이터가 12갈래로 나뉘어 동시에 처리됩니다. 파티션을 몇 개로 둘지는 토픽을 설계할 때 정합니다.

### 메시지는 어느 파티션으로 가는가 - 키(key)의 역할
"왜" 파티션을 나누는지는 알았으니, 이제 "어떻게" 메시지가 특정 파티션에 배정되는지를 봐야 합니다. 그 열쇠가 메시지에 붙는 **키(key)** 입니다. 프로듀서가 보내는 메시지는 보통 키와 값(value)의 쌍으로 되어 있는데, 이 키가 파티션 배정을 좌우합니다.

- **키가 없으면(null):** Kafka의 기본 파티셔너가 파티션을 알아서 정합니다. 흔히 "한 건씩 번갈아 가며 모든 파티션에 골고루 뿌린다(라운드 로빈)"고 생각하기 쉽지만, **Kafka 2.4부터의 기본 동작은 다릅니다.** 현재 기본 파티셔너는 **Sticky Partitioner(스티키 파티셔너)** 로, 잠깐 동안은 **한 파티션에 메시지를 모아 한 묶음(batch)으로 보낸 뒤** 다음 묶음에서 다른 파티션으로 옮겨 갑니다. 메시지를 묶어 보내면 전송 효율이 좋아지기 때문입니다. 그래서 짧은 순간만 보면 한 파티션에 몰리지만, 시간이 지나면 여러 파티션에 **고르게 퍼지는** 결과가 됩니다. 어느 파티션으로 가든 상관없을 때 쓰는 방식입니다.
- **키가 있으면:** 키를 해시(hash, 일정 규칙으로 숫자로 바꾸는 계산)해 파티션을 정합니다. 그래서 같은 파티셔너를 쓰고 파티션 개수가 그대로인 한, **같은 키를 가진 메시지는 항상 같은 파티션**으로 들어갑니다.

이 점이 앞서 말한 "순서는 파티션 안에서만 보장된다"와 직접 연결됩니다. 예를 들어 차량 한 대(트럭 ID `T-7`)의 GPS 좌표가 보낸 순서대로 처리되어야 한다면, 그 차량의 모든 메시지가 같은 파티션에 들어가야 합니다. 이때 **트럭 ID를 키로** 쓰면 `T-7`의 데이터는 모두 같은 파티션에 모여 순서가 지켜집니다. 반대로 키 없이 보내면, Sticky Partitioner가 묶음 단위로 파티션을 바꾸므로 같은 차량의 좌표가 여러 파티션에 흩어져 순서가 뒤섞일 수 있습니다.

> 같은 그룹의 메시지를 순서대로 처리해야 한다면, 그 그룹을 식별하는 값(트럭 ID, 주문 ID, 고객 ID 등)을 키로 지정하세요. 키가 파티션을 고정해 순서를 지켜 줍니다. (키에 따른 정확한 배정 방식과 acks는 4강에서 자세히 다룹니다.)

### Offset - 파티션 안에서의 순서와 위치
파티션 안에 메시지가 쌓일 때, 각 메시지에는 0부터 1씩 증가하는 번호가 붙습니다. 이 번호가 **Offset(오프셋)** 입니다. 오프셋은 파티션 내에서 그 메시지가 **몇 번째에 있는지(위치)와 순서**를 가리킵니다.

오프셋을 이해할 때 꼭 기억할 점이 있습니다.

- **순서는 파티션 안에서만 보장됩니다.** 같은 파티션 안에서는 메시지를 오프셋 순서대로 읽지만, 서로 다른 파티션 사이의 순서는 보장되지 않습니다.
- **오프셋은 파티션마다 따로입니다.** 파티션 0의 오프셋 2와 파티션 1의 오프셋 2는 전혀 다른 메시지입니다.
- **오프셋은 재사용되지 않습니다.** 앞쪽 메시지가 보관 기간이 지나 삭제되어도, 번호는 계속 증가만 합니다.

오프셋은 또한 컨슈머에게 책갈피 역할을 합니다. 컨슈머는 "내가 이 파티션을 오프셋 5까지 읽었다"를 기록해 두고, 잠시 멈췄다가 다시 시작해도 그 자리부터 이어 읽을 수 있습니다. (이 책갈피를 저장하는 동작을 오프셋 커밋이라 하며, 5강에서 자세히 다룹니다.)

### Broker - 데이터를 저장하고 제공하는 서버
지금까지 말한 토픽·파티션은 어딘가에 물리적으로 저장되어야 합니다. 그 일을 하는 **Kafka 서버 한 대**가 **Broker(브로커)** 입니다. 브로커는 프로듀서가 보낸 메시지를 받아 오프셋을 매기고 디스크에 저장하며, 컨슈머의 요청에 그 데이터를 내어줍니다. 우체국의 한 지점(branch)에 해당한다고 보면 됩니다.

브로커는 보통 여러 대가 함께 동작하는데, 이렇게 여러 브로커가 모인 것을 클러스터(cluster)라고 합니다. 클러스터 구조와 안정성 보장(복제 등)은 3강에서 이어집니다. 이 강의에서는 "브로커 = 실제로 데이터를 저장·제공하는 서버"라는 역할만 분명히 잡고 가면 됩니다.

### 네 개념의 관계
네 개념을 한 흐름으로 정리하면 이렇습니다. **Broker(서버)** 안에 여러 **Topic(논리적 분류)** 이 있고, 각 토픽은 여러 **Partition(병렬 처리를 위한 조각)** 으로 나뉘며, 각 파티션 안의 메시지는 **Offset(순서/위치 번호)** 으로 식별됩니다. 즉 "어떤 브로커의 → 어떤 토픽의 → 몇 번 파티션의 → 몇 번 오프셋 메시지"라는 좌표로 모든 메시지를 가리킬 수 있습니다.

아래 계층도는 Broker가 Topic을, Topic이 Partition을, Partition이 offset 번호가 매겨진 메시지를 **품고 있는 포함 관계**를 안쪽 상자로 나타낸 것입니다. 가장 안쪽 상자가 한 파티션이며, offset 0부터 차례로 쌓여 맨 끝에 새 메시지가 추가됩니다.

```mermaid Broker가 Topic을, Topic이 Partition을, Partition이 offset 메시지를 품는 포함(계층) 관계
flowchart TD
    subgraph B["Broker - 데이터를 저장·제공하는 서버"]
        subgraph T1["Topic: orders"]
            subgraph P0["Partition 0 - offset 0부터 끝으로 쌓임"]
                M0["offset 0"]
                M1["offset 1"]
                M2["offset 2 (맨 끝, 새 메시지가 여기 추가)"]
            end
            subgraph P1["Partition 1 - offset 0부터 끝으로 쌓임"]
                N0["offset 0"]
                N1["offset 1"]
            end
        end
        subgraph T2["Topic: payments"]
            subgraph Q0["Partition 0 - offset 0부터 끝으로 쌓임"]
                R0["offset 0"]
                R1["offset 1"]
            end
        end
    end
```

## 핵심 요약
- Topic은 같은 종류의 이벤트를 모으는 이름표 붙은 분류 단위이며, 수정·검색이 안 되고 끝에 덧붙이기만 하는 로그다.
- Partition은 Topic을 여러 조각으로 나눈 것으로, 병렬 처리와 수평 확장을 가능하게 한다.
- 메시지는 키(key)에 따라 파티션에 배정된다. 키가 없으면 Kafka 2.4+의 기본 파티셔너(Sticky Partitioner)가 묶음 단위로 파티션을 정해 시간이 지나며 고르게 퍼지고, 같은 키를 쓰면 (같은 파티셔너·같은 파티션 수에서) 항상 같은 파티션으로 가 순서가 지켜진다.
- Offset은 파티션 안에서 메시지의 순서/위치를 가리키는 번호이며, 순서 보장과 컨슈머 책갈피는 파티션 단위로만 의미가 있다.
- Broker는 데이터를 저장하고 제공하는 Kafka 서버이며, 여러 브로커가 모여 클러스터를 이룬다.
