Kind: captions
Language: en
Hello and welcome back to my channel. My
name is Piyush in this video we'll be
looking into custom resources and custom
resource definition in Kubernetes.
A little disclaimer
uh in this video as well like what we do
always there will be no editing, there
will be no cuts, no trims. So if I make
a mistake I'll try to resolve that while
the recording.
And if I have to do troubleshooting, I
will do that during the recording as
well and it will be published in the
original video. So if you have a short
retention span or if you have a lot of
time to go through the entire video
probably you are on the wrong platform
or on a wrong channel. Uh I would
suggest you to skip the video.
Otherwise, if you are someone who is
interested in learning how the concept
work from the ground level
uh that definitely let's start with the
video.
Okay.
So um in this video first let's go
through the custom resources like uh
what is this concept because this is one
of the recently introduced topic in CK
exam.
Um so that's why we are covering it. Now
what exactly is this custom resources?
So you see in Kubernetes we have many
out of the box objects that is available
by default to you when you do any
Kubernetes installation whether on cloud
or on premises or any way possible.
There are some default objects that's
created for you such as pod, deployment,
config map, secret, namespace, replica
set and there are many more.
So these are your out of the box objects
in Kubernetes.
But what if you have to create let's say
you have a requirement in which you need
to create a custom resource which is not
already present in the Kubernetes
installation.
And this is something that you have a
very specific use case for which you
have to create a new resource type
itself not the resource
uh not the pod, deployment or container.
I'm talking about a new resource type
like these are the resource types.
So what if you want to create a new
resource type as per your requirement?
So yes, you can do that by extending
Kubernetes API. What do we mean by
extending Kubernetes API? Means whatever
is already there you create your own
um custom resource or you can create
your own logic, you can create your own
set of instructions and then you attach
it to the Kubernetes. Okay? So that
means you are using the existing
Kubernetes but then you are adding
something on top of it. You are
extending the capabilities of your
Kubernetes cluster by extending its API.
Okay? So don't get confused when you see
that term how do we extend Kubernetes
API or how do we extend the capabilities
of extending existing Kubernetes. So
this is how you can do that.
Okay?
So
um you can create a new resource type,
okay? But why do we need to do that?
Okay? If Kubernetes already provide you
so many resources um these many and
there are lot many.
Okay, I need to just pause the recording
for a minute.
Okay. So what we are talking about is we
need to extend Kubernetes API, we need
to add a new resource type. But why do
we need to do that? Okay, so there would
be probably many scenarios that are not
already covered by open source
Kubernetes. Right, let's say you have to
create a new custom logic, you have to
create a new resource type, you have to
let's say monitor your resources using
the Kubernetes itself or let's say you
have to
uh implement GitOps within your
Kubernetes without using any additional
tool by creating your own set of
automations.
All those scenarios that are not already
covered by open source Kubernetes, you
can definitely cover those by writing
the code for it, by writing your custom
resources for it. Okay, so that's why we
use custom resources and custom resource
definition.
Um so when you create a deployment so
let's uh
let's try to uh relate the custom
resource with the existing resources.
Okay, so let's say you have an existing
resource within Kubernetes, a deployment
or a pod whatever.
So when you create a deployment
manifest, okay, so you create a YAML
file with all with all the required
fields such as image name, container
container container specifications
um metadata, kind, API version and so
on. So all the required fields of a
deployment when you do that Kubernetes
does the validation of those fields, all
the fields
with a resource definition of the
deployment. Okay? So you cannot just put
container image anywhere in the YAML,
right? And you cannot just uh add your
own fields that are not there in the
definition of the deployment. It has to
exactly match with the
deployment uh resource definition,
right? So this is a template.
Right? So when you create a deployment
or when you create any Kubernetes
resource, you create as per the
deployment. Okay, so the fields has to
match exactly the same as the template.
Also
it has to be at the same place, it
should have the same uh data type, it
should have the same set of uh
instructions, right?
So this is what happens when you do a
deployment. When you create a deployment
or any existing Kubernetes resource.
When you create a custom resource, it
works similarly, right?
So you create a custom resource
definition. Okay, so this definition is
what? It's a template. Okay, so this
template will have the
um
uh
Sure what's happening. Okay, so this
all the different fields such as like
name of the field, all the fields of the
custom resources, how it should be
validated, all the supported version,
data type of those fields like whether
it is string or it is a list or it's a
text or a number, an integer, whatever,
right?
So all those details will be placed in
the custom resource definition and then
you create a manifest of custom
resource. Okay, like we create manifest
of a deployment, it could be a YAML
file, a JSON file or you do that through
the cube CTL command in the imperative
way.
The same way you create the manifest of
a custom resource and then Kubernetes
does the validation of all the fields in
the custom resource with a custom
resource definition. Okay? So here what
we do the CR to CRD mapping.
So custom resource, the resource the
custom resource that you create and then
it has to be based on the custom
resource definition.
So custom resource is nothing but an
instance of the CRD. Okay?
So CRD is a template and based of that
template you create a custom resource.
Simple.
Okay, so um I'm sure it the basic
concept would have been cleared by now.
So that's why I was putting a lot of
emphasis on that. That's why we have
spent almost 7 minutes just talking
about the custom resource so that it's
clearly uh understood while before we go
forward, right?
Okay. So there are three main objects in
the um custom resource when you create.
So it has a CRD which is a custom
resource definition, the one that we
talked about. It has CR which is the
custom resource. This is nothing but an
instance of CRD.
And then we have a custom controller.
Custom controller what it does, it
manages the life cycle of the custom
resource. Okay? Like when you create a
deployment, okay? So deployment is
controlled by something called as a
deployment controller.
Okay, what this controller does?
First of all, what is a controller?
Controller is something that manages the
life cycle of your object. It makes sure
that your
desired state is always met. Make sure
that if you have two replicas in your
deployment, your
uh two replicas should always be up and
running. So this is done by the
deployment controller. So similarly when
you create a custom resource, you have a
uh custom controller option that you can
use. So you create your own controller
and
uh you actually instruct this controller
to manage the life cycle of your custom
resource. Okay? So this is optional but
this is recommended way this is
something that we do in a production
environment or in an organization. Okay?
So like this is the same example that we
have uh just discussed. For example, if
there are three pods in a deployment, so
it's the job and responsibility of a
deployment controller to watch this
deployment and make sure that all the
pods are running all the time. If there
is a crash, if there
one of the pods get um
stuck somewhere, stopped. Uh so it's the
job of a deployment controller
to instruct kubelet to start the or to
restart the pod.
Okay? So let's understand uh a high
level diagram of a custom resource and
the life cycle of it like uh how do we
create it, what are all the
different components and how they are
related to each other. Okay?
So for example, a DevOps engineer uh
creates a custom resource definition.
So this custom resource definition is
what? It's a template. When you create a
deployment you don't have to create uh
the deployment definition because it's
already been created when you installed
or when you uh you know, installed your
Kubernetes cluster.
But in case of a custom resource,
because it has to be custom, it is as
per your use case. So, a DevOps engineer
Then, a developer or a user or an
operator, whoever is uh
maintaining the application, creates a
custom resource, which is based on the
CRD.
So, let's say um there are certain
fields in it, such as
replicas,
uh there is version, there is release,
If there are these fields inside your
custom resource, so it has to be there
in the custom resource definition at the
exact same place. Okay? Only then the
validation will be passed.
So, once you create the custom resource,
uh Kubernetes does the validation with
the CRD, and you will get the error if
the field does not match or if you
create something that is not as per the
definition or the template.
And then,
DevOps engineer also creates a custom
controller. Okay? This is an optional
step, but you can also create a custom
controller that watches your custom
resource all the time.
Okay? It makes sure that um the desired
state is always met. Okay? It makes sure
that whatever uh it supposed to do, it
is doing its work, and it makes sure
that um
uh it is as per the requirement, it is
as per how you created the definition.
So, uh I think that's uh do do do do do
do Yeah, that's uh the definition part
of it. That's the theory part of it. We
now jump into the demo of uh custom
resources and custom resources
So, let me just share my screen, and I'm
going to put this away.
Okay. So, um I have written down the
instructions uh very clearly in the
GitHub repository. You can follow along
or you can practice it from there, but I
would still suggest to watch the video,
because I will be explaining some of
these as we go along.
Okay? So, I have also a lab uh a a
cloud code cloud lab that I will be
using.
Uh but, you can use your own um
you can also you you can also use your
own Kubernetes installation. It could be
Minikube, kind, any managed cloud
service, anything will be fine. Okay?
So, uh this is the one that I will be
choosing. Let's start the lab, and uh
so, I'll be following these instructions
so that I have written over here.
So, first uh we'll clone this sample
repository, uh which has a sample
controller. We will not be installing
controller in this video.
Um it'll be a little out of scope, plus
uh I like creating your custom
controller or using it uh would be out
of scope from CK perspective, but I just
clone that but I can use the other
manifest from it. Okay?
So, I'm going to use git clone and CD
into sample controller.
Okay? Now, I'm going to create the
custom resource definition from this
YAML. So, let's open this YAML, and let
me show you what does it have.
Okay, uh let's clear the screen, and I'm
going to just expand this a little
further. Let me just zoom in a little
bit. Okay, I hope this is visible.
Okay, and let's
increase the size.
Open in a new window, maybe. Yeah, this
is much better.
Okay, so let's do a VI on the uh
subresource.yaml
that we Oh, it's not there. Am I not in
the current directory?
Oh, because I have Yeah, let's go inside
sample controller,
let's do a VI on the artifacts. Okay.
So, what does it have?
It has a resource of kind custom
resource definition, as you can see.
The name is
foo.samplecontrollers.k8s.io.
Okay? So, uh this is the name of the
custom resource. So, when you do, let's
say,
uh when you when you will do get custom
resource definition, you will see uh
this name over there. I'll show you
that. Then, it has some annotations, and
it has the specifications. Okay?
Specification in this will provide all
the details, such as the version that it
will support. So, as we can see, we have
set the version as v1alpha1.
Okay? Then, you go down, you check the
other spec the specification, like
whether it is served, uh storage is
true, you can use the storage along with
it.
And then, we have the specifications
over here. This specification, like we
have specification in a deployment YAML
or a pod YAML. So, this uh these are all
the fields inside that. What are all the
fields? So, we have deployment name,
okay, which is of type string. We have
replicas, which is of type string. We
have minimum and maximum number of
replicas. Okay? So, it has
fields as deployment name and replicas.
So, these are
two specifications of that custom
resource definition. Okay?
And then, it'll have the So, there is
this metadata that get generated
automatically, right? Uh you don't write
it, but when you create the object, you
will see a status field in the YAML.
So, in the status field, you will see
available replicas, which is the
type of integer. Okay?
And then, have name, which is of kind
foo, plurals is foos, and this is a
namespaced
uh scoped object. So, it could be a
cluster scoped or a namespace scoped,
So, this is a namespaced scoped object,
and you can also find it with the name
foo. So, the kind is foo, like we have
kind as deployment, kind as pod. So,
this kind is foo.
So, let's apply this YAML. I'm going to
use kubectl apply oops.
Okay, let me just uh change the prompt
size to something
shorter and something that is easily
readable on the screen.
Uh
let's see. Oops, what did I do?
Okay, it should be I guess PS1.
No, probably I cannot set it over here,
or I'm doing something wrong.
See, PS1 is equal to
Um
No, it's not working, I guess, over
here.
Anyways.
So, let's just apply that YAML. kubectl
apply -f and
where was the YAML?
Okay, so either create or either apply,
both will work.
Okay, it says created. Now, if I do
kubectl uh get CRD.
Okay? So, you will see your custom
resource definition. So, these are the
already existing one, but this is the
something that you have just created.
And it's name is
foos.samplecontrollers.k8s.io.
So, if I do kubectl get foo,
you won't see anything, because we have
not created the custom resource yet.
Okay? We just have created the
definition of it. Okay?
So, let's proceed further. Uh this is
what we have checked already.
Let's create the custom resource of type
foo. Before that, let's check the YAML.
What's there inside it?
Okay, so VI and this. Okay? So, it has
two fields. So, the API version is
sample controller k8s.io, and uh the
version is v1alpha1. So, this is
something we have already seen in the
definition. Okay?
This is your uh group. This is your
version. Okay? Then, kind is foo. Okay?
It was there, and then metadata name is
example foo. You can give it any name
you want.
And then, these two were the fields
inside uh that custom resource
definition, the deployment name
and the replicas. Replicas minimum was
one, maximum was 10. So, if you
enter anything more than 10, okay, in
fact, let me show you. Let's put uh 20
replicas.
apply the changes. kubectl apply -f and
and this YAML.
Okay? So, what it says?
The foo is invalid. Spec.replicas
invalid value 20. Spec.replicas in body
should be less than or equal to 10.
So, this is giving you the validation
error, because as soon as you applied
the YAML, it does the validation of your
custom resource with the custom resource
And it see, okay, there is a failed
validation. Okay? So, the replica should
be between 1 and 10, but it's 20. It's
not valid. Uh you cannot create the
object unless you
change the values. Okay? So, let's make
the change again, and let's keep it,
let's say, two.
Okay, let's apply it again.
And that's it. Says it's created. Now,
if I do k get foo,
Okay? You see, there is an object of
type um
a object with the name example foo and
it's just been created. This is the CRD
Oh, sorry. This is the custom resource
that you have created.
Now, you can uh verify if you have used
controllers, then you will see the
deployment as well. Uh but, we have not
used controllers, so this is just we
wanted to check.
Now,
from the exam perspective as well, like
they will ask you to uh check different
fields. Uh and if you again have to uh
do like this is because this is a type
of a Kubernetes object, right? So, you
can do a describe on it like how you do
any other object. So, if I do cube CTL
describe
Oops.
Let me just
Okay. cube CTL describe
uh CRD foo.
So, it will show you uh different
things. Um
like all the different messages.
Accepted name kind foo foo list and but,
this is accepted and this is replicas is
1 to 10. This is based on
um the CRD.
Okay. And all the different
specifications of it like that you have
used while creating it. The name is
uh foo.
Actually, this is foo is a CR not a CRD.
So, if you
Let's remove this. Describe CRD and then
you need to give the CRD name. Now, it
is showing all the CRDs. The one that we
are looking for is this one sample
controller No.
Where is the name?
Kind metadata uh group name
Okay. Let's do cube CTL get CRD.
So, CRD name is this
foos.samplecontroller.k8s.io
and if I do
describe CRD and the CRD name, then it
will show you all the details.
Um now, you can check all the details.
This is name is foos.
sample controllers.k8s.io.
Okay. Now, if you want to check the
details of the custom resource, so cube
CTL get CR won't show you anything.
Custom resource won't show you anything
because the type is foo.
Okay. And if I want to do a describe on
it, so cube CTL describe foo, which is a
resource type, and then the resource
name.
Okay. So, it has this aspect, which is
deployment name is this, replicas are
two. You won't see any uh pods or you
won't see any deployment because we have
not created the controller logic to
watch this and to create the deployments
or the pods for you. That's why it's not
visible. Okay.
But, as part of exam again, uh
you will be asked to describe and you
know, all those things that we have just
did. Plus, there'll be one more task to
check the fields of CR. For example,
let's say in the exam if it's been asked
to
is to ask to show the document for
deployment name of the specification.
Okay. So, for that, we know to check any
all the API version related details, we
use the command cube CTL explain and
then the
name of the resource. Okay. So, cube CTL
explain foo.
And now, we need
uh the field from the specification.
Okay. So, this is the group. This is the
version. This is all good. But, to get
the particular field, we do foo.spec.
Okay. dot the name of the field. Or if
we do uh foo.spec, we'll see uh all the
fields. Okay. Over here, we see
deployment name, replicas, and so on,
right? But, if we need to get the
details from one particular field, so
foo.spec.deployment
name. Okay. So, now it is just showing
that particular field. Deployment name
should be of type string and all the
details around it. Now, you will be
possibly asked to redirect to a file, so
which you can do that with the uh
redirection and then the file name, and
that should be it.
Okay. I guess uh that's probably it from
the exam point of view. These are the
exam questions that we have uh just
discussed. For example, if it's been
asked to
um extract the documentation of the
subject field, this is how you can do
that. Cube CTL explain certificates. So,
certification is certificate is the type
of object over here.
Okay. Uh if you want to do that for the
Like if there is a certificate manager
type of CRD installed already, so you
can do that.
And if um
In fact, let me just show you how to do
that
so that it'll be easier for you. Okay.
So, what we'll do is we'll install uh
certificate manager
as a custom resource using helm. Okay.
So, first you will do the helm repo add.
Okay. So, let's do clear the screen.
Okay. And I'm going to run this command
to add the certificate manager repo.
Okay. And then I'm going to use this
command
to install the chart.
Okay. I'm sure you have watched the helm
uh helm chart video and all the
different fields of it. If not, you can
check it out. It's in the same playlist.
Uh I believe there is video somewhere in
40s. Uh you can check it out. So, there
is this field uh that I want you to pay
attention on. So, we have different helm
install chart name release uh sorry,
release name, chart name, and so on
repository with the name space and
create name space if it does not exist,
version.
And then there is this field set
CRDs.enable equal to false. That means
it's we are instructing helm to not
install the CRDs. Let's say we have
installed CRD from some other resource
or we have
installed our own CRD. If we are
instructed helm to not install the CRDs
and install rest of the component. So,
for that, we keep it as false. Okay.
But, for now, let's keep it as true so
that it should also be installed.
But, make sure you understand this
concept. This is important from the exam
point of view.
Um this is how you can
install the any helm chart without the
CRD or excluding the CRD.
Once
this is installed, um
we can check different fields of
certificate manager. Okay.
Okay. It says is installed. So, now if I
do cube CTL get CRD.
Okay. So, a lot of other CRDs have been
installed. So, you can just filter it by
using a grep on certificate.
Certificate. Okay. So, we have
certificate request, certificate uh
cert.
Cert manager.
Um there'll be another video. I guess
this is part of operator somewhere
wherein I will explain everything about
cert manager and install it properly.
But, for now, just from the perspective
of CRD, I'm explaining. Okay.
So, um this is how you can check the CRD
of certificate manager. Now, if uh
you have been asked to explain a
particular field from the documentation,
let's say First, let's do a cube CTL
get certificates.
Wait.
Yeah, it's hard to do recording at home.
There is always someone at the door
knocking. But, good thing is I've got my
key.
I've got my tea. I was supposed to say
tea. I said key.
Okay. So, let's come back and let's see
cube CTL get certificate. Okay. Because
uh this is the CRD. Okay. But, it's um
it's of type certificate. Okay.
So, I guess certificates. Okay. Let's
check the CRD.
Certificates.
It should be certificates.
Yeah, certificates are cert managers. Do
do do do do do Oh, maybe because we have
not created the certificates.
Yeah, it has some additional steps,
which we'll cover later on. Okay. I
don't want to uh unnecessary drag this
video further. So, this will be covered
in the operator section. We need to
create a certificate so that it can
issue the certificate. But, for now, uh
if Yeah, for now just if you want to
let's say explain the documentation for
that certification, this is the command
that you would use. This is the same
that uh one what we use for the foo
resource type. Okay.
So, this certificate has these many
fields. So, countries, locality,
organization, and so on. Okay.
So, um
Group is certificate cert manager. Kind
is certificate. And even though there is
no certificate installed, you can still
run cube CTL explain
because it's coming from the CR
definition. Okay.
So, cube CTL explain certificates, which
is the resource type
.spec .subject. Subject is the field
that we were looking for.
Okay. So, uh over here
we needed to get the documentation of
subject specification field, and this is
how you can get it. Okay. So, you can
check it. You can use these debugging
commands, uh which we have already seen.
And
yep.
That's it for this video. I hope you
found it helpful. I hope you found it
valuable.
If yes, uh thank you so much for
watching. You can subscribe the channel
if you want. You can hit the like on the
video if you want to
support the channel, and I will see you
soon with the next one. Thank you so
much.