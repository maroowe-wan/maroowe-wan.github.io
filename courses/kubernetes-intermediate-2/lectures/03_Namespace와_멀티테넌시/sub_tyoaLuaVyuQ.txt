Kind: captions
Language: en
hello everyone welcome back to the channel&nbsp;
I am Nikhil Kumar in the last video we have&nbsp;&nbsp;
explored how resource quota helps us control total&nbsp;
resource usage per namespace but what happens if&nbsp;&nbsp;
your developers forget to define resource request&nbsp;
or limits in the first place that's where limit&nbsp;&nbsp;
ranges steps. Imagine we have a team where every&nbsp;
engineer books any hotel they want for a business&nbsp;&nbsp;
trip but we need a rule like a maximum 5,000&nbsp;
per night we have a limit to book any hotel&nbsp;&nbsp;
that's what limit ranges do they define default&nbsp;
minimum and maximum resource usage per container&nbsp;&nbsp;
or a pod in a namespace so basically how limit&nbsp;
ranges work limit ranges are namespace scoped&nbsp;&nbsp;
you can set default resource request or limits and&nbsp;
minimum and maximum bounds kubernetes will apply&nbsp;&nbsp;
these defaults automatically if a pod doesn't&nbsp;
define them this is perfectly complimentary to&nbsp;&nbsp;
resource quota which enforce total namespace usage&nbsp;
now that we have understood what is limit ranges&nbsp;&nbsp;
and how limit ranges work let's see it in the&nbsp;
action so this is our kubernetes cluster first we&nbsp;&nbsp;
will create a resource called limit range for that&nbsp;
we will create our manifest file let me create a&nbsp;&nbsp;
manifest file for the same that is limitrange.yaml&nbsp;
so first of all we need to specify the API version&nbsp;&nbsp;
that is v1 then we need to specify the kind&nbsp;
that is limit range then we need to specify&nbsp;&nbsp;
the metadata inside this we are going to specify&nbsp;
the name and the name space name is default limits&nbsp;&nbsp;
then we need to specify the namespace so here I'm&nbsp;
going to specify dev namespace now here we need to&nbsp;&nbsp;
specify the specification spec and we need to set&nbsp;
the limits so first we need to specify the default&nbsp;&nbsp;
limits so default will be CPU which is 500m and&nbsp;
then we have a memory that is 512 Mi similar to&nbsp;&nbsp;
this we are going to specify the default request&nbsp;
default request and default request is CPU which&nbsp;&nbsp;
is 250m and memory which is 256Mi which is&nbsp;
applicable to type that is container basically&nbsp;&nbsp;
if a container in a dev namespace does not define&nbsp;
any resources Kubernetes will apply these default&nbsp;&nbsp;
request to that particular container let me apply&nbsp;
this kubectl create -f limitrange.yaml limit range&nbsp;&nbsp;
got created let's describe this kubectl describe&nbsp;
limit range and the name of limit range that is&nbsp;&nbsp;
default-limits and namespace that is dev as we&nbsp;
can see we have a limit that is default limits&nbsp;&nbsp;
which is having a default request for the CPU&nbsp;
that is 250m and default limit is 500m similar&nbsp;&nbsp;
to this we have a default request for the memory&nbsp;
that is 256Mi and default limit is 512Mi and it is&nbsp;&nbsp;
applicable to containers now let's test this by&nbsp;
creating a pod without specifying any resources&nbsp;&nbsp;
so let me create a manifest file for the same&nbsp;
vi pod-test.yaml so API version V1 kind is pod&nbsp;&nbsp;
and metadata name is test pod and name space&nbsp;
which is our dev and then we have a spec so&nbsp;&nbsp;
inside this I'm just specifying the containers&nbsp;
so I'm going to create one container with nginx&nbsp;&nbsp;
and the image is nginx let's create a pod using&nbsp;
this kubectl create -f pod-test.yaml the pod got&nbsp;&nbsp;
created now let's check what kubernetes is applied&nbsp;
automatically so kubectl get pods n dev it's under&nbsp;&nbsp;
dev namespace kubectl describe pod test hyphen pod&nbsp;
h - n dev namespace so container is running and&nbsp;&nbsp;
we can see it automatically applied the default&nbsp;
request and the limits which we applied via the&nbsp;&nbsp;
limit ranges so limit ranges is very useful when&nbsp;
when any user is not specifying any request or the&nbsp;&nbsp;
limit in the pod definition file and if we run&nbsp;
the same command to describe the limit range we&nbsp;&nbsp;
can see this is the same default request and the&nbsp;
limit we have specified in the limit range in my&nbsp;&nbsp;
last video we have covered the resource quota now&nbsp;
it's time to combine both that is resource quota&nbsp;&nbsp;
and the limit ranges together so in this I've&nbsp;
already created a limit ranges with the default&nbsp;&nbsp;
limits and the request now let's create a resource&nbsp;
quota i will use the same manifest file which I&nbsp;&nbsp;
used in my last video that is resource-quota.yaml&nbsp;
so this is the same manifest file which I used so&nbsp;&nbsp;
I'm going to use the same to create a resource&nbsp;
quota as well in the dev namespace kubectl apply&nbsp;&nbsp;
-f resource-quota.yml so resource quota got&nbsp;
created let me verify that it's under the dev&nbsp;&nbsp;
namespace so now we have a resource quota and the&nbsp;
limit ranges both now let's try to launch 11 pod&nbsp;&nbsp;
in the dev name space so in this we have a limit&nbsp;
that is we can create up to 10 pod and one pod is&nbsp;&nbsp;
already running in this and we can utilize we can&nbsp;
request CPU up to 2CPU and memory up to 2GB so for&nbsp;&nbsp;
that and we have a limit ranges as well so let's&nbsp;
try to create a pod using this so limit range will&nbsp;&nbsp;
request 250m CPU and 256 mi memory by default so&nbsp;
here I'm going to use a for loop command so this&nbsp;&nbsp;
particular command will create a 11 pod with&nbsp;
a different names let's run this we can see&nbsp;&nbsp;
the seven pod got created but the rest of the pod&nbsp;
is getting failed why because we have a resource&nbsp;&nbsp;
quota limit which we have hit by this particular&nbsp;
command if you see here in this particular command&nbsp;&nbsp;
we are getting an error while creating a pod&nbsp;
that is we are exceeding a quota that is a team&nbsp;&nbsp;
a quota so basically resource quota is a namespace&nbsp;
level budget it will be applicable to the complete&nbsp;&nbsp;
dev namespace just like we saw in our demo but&nbsp;
limit ranges will be applicable to each pod or&nbsp;&nbsp;
the container and it applies default limit and&nbsp;
the default request together they give you full&nbsp;&nbsp;
control and safe boundary in multi-team cluster&nbsp;
that's it for today now we have seen how limit&nbsp;&nbsp;
ranges helps us to set defaults and how they work&nbsp;
in perfect harmony with resource quota if you like&nbsp;&nbsp;
this video share it to your friends and subscribe&nbsp;
for more content like this i will add a link of&nbsp;&nbsp;
resource quota video in the description area as&nbsp;
well thanks for watching see you in the next one