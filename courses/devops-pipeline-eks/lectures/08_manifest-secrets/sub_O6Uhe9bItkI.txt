Kind: captions
Language: en-IN
Today our topic is configMap.
Agenda of this video are, what is configMap?
How to create configMap from yaml file? and
the second one is from command line?
Then we will try to learn how to insert the
variable into container.
Again, we can insert the variable from environment
variables and volume.
Inside this environment variable, we can divide
it into three parts, from the file or directory.
2nd is from yaml file, and inside this, we
have three types, env, envFrom and from command
line.
So, both are the syntax here, env and envFrom.
And we will try to discuss what are the difference
between env and envFrom.
ENV Stand for Environment variable.
And inside this volume, we will create through
yaml file.
So, let's explore the first topic.
That is, what is configMap?
A ConfigMap is an API object used to store
non-confidential data in key-value pairs.
We have already covered what is key-value
pairs in the Yaml file topic.
It means that, ConfigMap does not provide
secrecy or encryption.
If you want to store data confidential then
use a Secret rather than a ConfigMap or use
additional (third party) tools to keep your
data private.
Kubernetes provides these values to your containers.
Pods can consume ConfigMaps as environment
variables, command-line arguments, or as configuration
files in a volume.
It means that we can insert the extra parameters
from three ways, and we will cover in the
lab.
A ConfigMap allows you to decouple environment-specific
configuration from your container images,
so that your applications are easily portable.
In short, we can say that use a ConfigMap
to keep your container’s application code,
separate from your configuration.
A ConfigMap is not designed to hold large
chunks of data.
The data stored in a ConfigMap cannot exceed
1 MiB.
If you need to store settings that are larger
than this limit, you may want to consider
mounting a volume or use a separate database
or file service.
Just for your information, the configMap is
stores in the ETCD database.
In short, we can say that, if we need to add
environment variables like hostname or some
other variables, we can use ConfigMap.
How to create configMap.
There are two ways to create configMap.
The first one is through yaml file, and the
second one is from command line.
Let's explore the first one.
That is create a configMap through yaml file.
In the configMap, kind equal to configMap
here, C and M are capital words, API version
is v1.
Under the metadata section, we have defined
name of this configMap, so that we can identify
it.
After that, after that we have added "namespace
= core", it means that this configMap will
be created inside the core namespace, and
please bear in mind that configMap is a namespace
object.
Further, you will notice the data section,
however for other objects, we have always
spec section.
Inside this data section, we have added 3
variables.
Remember the key-value pair.
NGINX_PORT is key and 8080 is the value.
Same as it is language is key and the value
of this key is English.
In the last variable, we have defined more
than one values separated by comma.
Now, let's explore the second one that is
create a configMap from command line.
In order to create the configMap from command
line, I just take a reference from previous
yaml file, so that we can understand it better.
First, we need to use kubectl create sub-command
&amp; then object type is configMap after that
we mention the configMap name, that is, “command-configmap”.
Next, we use –-from-literal option to specify
the key-value pair.
If we have multiple key value pairs, then
we can use multiple time this option like
this and like this.
If I describe this configMap, I will observe
same output if we create a configMap from
From here we have data.
Inside this data we have key, and this key
has the value of 8080.
Then again, we have key and the value is this.
Again, we have key, and the value is English.
Inside this binary data, we have not mentioned
anything, so it means that in the configMap
we can insert the value inside this data or
binary data.
Now, we will talk about how to insert the
As I said previously, there are two ways environment
variables are from volume.
From the environment variable, we have two
options from files or directory from yaml
file env, and envFrom and from Command line.
So, let's explore the first one that is from
file or directory.
If you use directly file or directly with
the command option for now, just remember
that while using file in the environment variable,
the file name will be the key and the content
of this file will be the value.
So here the file name is NGINX_PORT.
So, this is my key, and 8080 is my value.
Don't worry, we have a dedicated lab for this
topic.
So, we will cover in the lab objection, how
we can create this thing.
So, for now, let's move forward.
From the command line we have env syntax,
so let's explore this one.
This is our simple deployment yaml file.
Or you can see that manifest file.
One can notice the kind equal to deployment.
In this spec section, we have some information
about the container.
If we want to insert the variable by using
env syntax, we have to adhere the parameters.
Let me zoom this section
Here, the first section is env.
Under this, we have defined the name of key
that we want to insert it.
Then we have value from.
Beneath this we use configMap, reference syntax,
and the name of our configMap is this.
Please bear in mind that the same name we
have mentioned in the configMap yaml file,
in the last, we have defined the key name
that is NGINX_PORT.
Must be same in the configMap yaml file.
Again, we are going to insert second variable
and the name of this key variable is Web-App-Lang.
We can use any name, no limitation.
In this example we are using the same configMap
is the reason we are using this configMap
name.
Please note that we can use other configMap
too.
There is no limitation to use only one configMap.
From this configMap, we want to insert second
key, i.e., language.
In the container, we will observe these two
variables’ names i.e., NGINX_PORT and Web-App-Lang.
Please note that we can use "env" option to
hardcode our key values.
In this example, we are not calling our configMap,
but we hardcoded our key1 and key2 variables.
So, key1 is this and the value of this key
is value1, just for the information.
This is the extra information apart from the
configMap topic.
So, let's move to our second syntax.
That is envFrom.
Again, this is our simple deployment YAML
file, and this is our spec section.
Inside this, we have added envFrom section.
Stands for environment variable.
Beneath this, we have a configMap key reference
(configMapRef), and here we must define the
name of our configMap that we created earlier.
Here, name is same.
That is “example-configmap”.
If we add entries like this, we will observe
the content of this configMap in the container
environment variables.
In this case, we will observe three environment
In last, we can say that if we want to consume
all the content of configMap, then we can
use "envFrom" syntax.
That is this one.
And if we want to insert some key, then we
can use "env" syntax.
So now I hope you understand the difference
between env and envFrom option.
So, let's move our third option that is from
command line.
Now this is our pod.
Why we are using pod because we are going
to use command option.
It means that this pod will complete the job
and stop, that's it.
I will talk about job in some other topic.
However, today we are just focusing on command
option, while using configMap.
In this spec section, we are using "env" option.
Please note that.
These two variables names, which we used earlier.
Inside the command option, here in the command,
we are just print the variable names.
We are not using key name, that is language.
That is our second variable.
So what we have defined the name of this key
we just printed here.
And when we print this variable, we will observe
the value of this key.
Therefore, we can say that in the pod’s
configuration file, we define one variable
name, and that variable name is pointing out
to one key, and that key has the value inside
this configMap.
I execute this command "kubectl create -f
Newpod.yaml".
So, in this yaml file, we have the content
of pod yaml file that I described in the previous
slide.
If I execute this command "kubectl get pod",
I will see this status is completed.
And if I use "kubectl logs configmap-example-pod",
this is the name of my pod, I will observe
“8080” and “English”.
Let me show you the command that I used here
is echo “NGINX_PORT” and “Web-APP-LANG”,
the value of NGINX_PORT is
“8080” and Web-App-Lang is “English”.
Now let's move to our second topic.
That is volume.
How to insert it, right?
So let, and we have only one option inside
this volume that is file.
Mounting a configMap as a data volume.
In this case, we are using configMap as a
file in the pod's container.
For this under spec section, we need to add
a volume section.
And also extend the container section and
add the volumeMounts name Should be same.
We had already discussed in the volume videos.
Inside the volume section, we need to add
configMap parameter and add the configMap
name, same as we define any configMap file.
Once we create the deployment, we will observe
3 files.
Inside the "/etc/config" directory in the
container, both yaml files are now in front
of you so, that you can visualize it and notice
that I have defined configMap name on both
deployment manifest files.
We have to make a link in the yaml file, deployment
yaml file so that we can instruct Kubernetes
object to fetch the content from the configMap.
And the last topic is immutable configMap.
So, what is this?
If we add the immutable equal true, it means
that once we created a configMap with this
option, we cannot upgrade the configMap in
future.
If we want to modify this configMap, then
we have to delete the existing one and then
create a new one with modified data.
If you want to add this option, please wisely
add this option and you are making sure that
we are not going to update this configMap
in future.
Now, let's move to “configMap lab + exam
preparation”.
As my best knowledge configMap question will
become under CKAD, not in CKA.
configMap directory is already created on
my Github page, so inside this "14 volume"
directory, going inside this configMap.
And we have 4 labs available.
First, I'm going to click command line.
It's very short one.
We have already covered all the parts.
So how we can create the configMap from Command
Line.
So here we are using "kubectl create configMap",
and the name of this configMap is Command,
configMap from "--from-literal" NGINX_PORT
Equal to 8080.
So, let me go here.
Copy,
there is no file inside this directory.
From the output, we can observe that this
configMap is created.
As you can see here, that command configMap
is created.
Describe from this describe sub command the
name of this conflict file is Command, configMap,
and it is created inside the namespace default.
We have not set any label and notations, and
we have data.
And here we have binary data.
So we have set variables inside this data.
So, NGINX_PORT, the value is this.
So, this is my key and this is my value.
This is my key, and this is my value.
Key value pairs.
Here, let me clear this screen.
After successfully completed the configMap,
now we need to create the deployment.
In this deployment I'm using envFrom, and
after that configMap reference, and this is
my configMap name, so come command-configMap.
This name is same here.
So let me copy this yaml file and paste it
over here.
Let me clear the window.
This is my yaml file.
Now, I need to create the deployment on the
above yaml file.
So, I'm just using this command.
From the output, it is clear that my deployment
is created successfully.
Check the deployment.
I can also check the deployment here by executing
the command "kubectl get deployment", one
of my pods is running successfully.
As of now, my 1 pod is running, so, let me
check this pod.
Pods grep this one and running and "awk ''{print
$1}'" . So now it'll print only one name.
And let's suppose if we have multiple pods
are running, we can use here "head -n 1".
So, now it'll always showing me only one pod
that is running.
What I can do here, kubectl exec -it, and
then dollar sign ($), and then bracket close
{}.
Inside this bracket, I'm going to use this
command.
Here and then env, it'll print env.
From the output, we can confirm these 3 variables
are correctly inserted inside my container.
This is what I did here in order to clear
the lab.
First, I need to delete this deployment, and
then, I need to delete this deployment, and
after that, delete this configMap and also
delete this yaml file.
So let me copy all these three commands and
clear the window, insert all these things.
"ls -ltr", everything is removed here.
We have completed the first lab from the command
How to create the configMap from command line.
This is the lab we have completed.
Now, we need to perform the second lab.
For this, I need to click on "02 Env variable.md",
How to insert the configMap as an environment
variable?
This is my configMap file.
Here we have defined 3 variables.
NGINX_PORT 8080, language= English, environment=
"production, deployment, and test".
After that, we have a deployment.
Here we are using envFrom, same as it is we
did for the previous lab.
Let me copy this and then, "ls" I need to
paste over here and then perform "ls".
Our file is created, create the object from
yaml file, so I'm just using "kubectl apply
command".
Once it's done from the output, we get to
know that our configMap, the name of this
configMap is "example-configmap" is created
and deployment.
The name of the deployment is "env-deploy"
is also created successfully.
So, we can describe this configMap
from the output.
We get to know that inside this data we have
key and the value is this.
This is my key NGINX_PORT and value is 8080.
So, this is key-pair.
Explore the configMap through get command.
We can also explore, let me clear the screen.
Here it is showing inside this data we have
three keys,
Login into 1 pod and check the environment
The last time I have already shown you what
is this, so, I'm just copy this command here.
And check the env inside 1 pod.
Here we can see that our 3 variables key and
the values attached in the container.
clear the lab.
So, I'm just going to delete configMap, yaml
file and deployment.
It'll be deleted and then remove this file.
Fair enough.
Let me execute "ls -ltr".
Now, the file is deleted and there is no configMap
should be here.
configMap, this is the default one.
If I go back and now, click on configMap from
file.
In the theory part, we have already covered,
so here I'm just going to create one single
file and NGINX_PORT, and the name of the file
would be the key.
And inside this file would be our variable
/value.
You can say that.
Let me paste over here.
After that, we are going to create a configMap
in "env-file".
This is the name of my configMap and from
file option.
This time we are using here, and we are saying
NGINX_PORT.
This is my key, created deployment.
And here again, we are using envFrom.
The third lab is also from envFrom Environment
Variable.
After creating the yaml file, we need to create
the objects, so for that reason, "kubectl
apply -f env-file-deploy.yaml".
Now, our deployment is created, and it'll
create some pods.
Let me check if my pods are running.
Yeah, some pods are running.
Here, I can use this command.
Now, I can see here that NGINX_PORT is showing
8080.
This is the variable that we have attached.
So, now we are going to delete all the stuff.
Clear the lab, everything is removed.
Let me go with "ls -ltr".
Nothing is here.
That's great.
Now, the last one is configMap from volume.
In order to proceed this lab for configMap
from volume, first I need to create a namespace
core, because here we are going to create
the configMap inside 1 core map, core namespace,
and inserting 3 variables.
In the deployment, again, we are using the
same namespace and inside this volume section,
we are using ConfigMap sub command syntax.
And the name of this configMap is "example-configmap1".
And the same name we are using here, "example-configmap1".
So, let me copy, before executing this command,
let me create 1 namespace
and paste over here.
After that, create the object from the yaml
So, it'll going to create 1 configMap and
1 deployment for us.
From the command output, we get to know that
it is successfully executed.
After that, explore the configMap through
describe sub-command.
We will see the same 3 environment variables
that we are going to attach here, key value
pairs, and this time we are using "-n core"
namespace.
So, here we will observe inside this data-
we have 3 keys.
Now verify our pod is created or not.
I have to use core namespace.
So all are created, all five pods are created
here.
If I execute this command, I will see the
ENV environment variable.
It'll print out, but this time it'll not showing
me anything here.
Why?
Because this time we are inserted the key
values as a file here.
You will observe these 3 files under the directory,
"/etc/config", I'm just going to check if
this is the case or not, why it is inside
this "/etc/config" directory?
Here, we have mentioned inside this container's
configuration mountpath is "/etc/config".
Let me paste this command here.
And now I can see here that 3 files are there.
We can also open this file by using the CAT
So let me copy this command and paste it over
Language is English here, so this is the way
we can insert the configMap inside our container.
So, I hope this is clear for you.
Let me clear the lab.
"kubectl delete namespace core".
Question: You are tasked to create a configMap
and consume the configMap in a pod using the
volume mount, please complete the following
question.
Create a configMap named another config.
The name of the configMap is another config
containing the key value pair, and the key
is key 3 and the value is 2.
Start the pod name.
And the name of the pod should be “nginx-configmap”
containing a single container using the "nginx"
image and mount the key you just created into
the pod under the directory "/some/path/".
So, we can create the configMap with the help
of command line "--from-literal" key value
pair.
So, if I copy here, and paste it over here.
My configMap is created.
I can check this configMap "kubectl get configMap
another-config".
1 key is here.
After that, I need to create a pod.
Here in the exams, you can open a terminal/
browser and write it here, "Kubernetes.io"
website and click on documentation and type
here, configMap.
Just click on the first one.
And if you scroll down, you will observe this
pod and if you scroll down more.
Yeah, this, copy this yaml file, create 1
file here "config.yaml", any file name, you
can write it here.
Press I for insert, shift + Insert now as
per the question.
What we need to do here, the name of the pod
is nginix and in the Exams you need to use
shortcut control shift + C and paste it over
here,
right?
And after that, what it says that using nginx
image.
Here, it is using "redis", so I'm just modify
it here and "/some/path/" directory.
mounted in "/some/path/".
So, I'm just going to remove this one and
"readOnly" is true, if you want, you can delete
And the name of our configMap is another-config.
Just copy this one
and paste it over here.
Apply this one "kubectl apply -f configmap.yaml".
We added as a volume.
So it should be visual inside the "/some/path/"
So let me go here.
"kubectl get pods".
Yes.
My pod is running.
Let me go inside this pod here "kubectl"
If I open this file, I can see this "value2".
I can do like this here.
So now it is showing I neatly "value2" and
the same thing I have done for you.
So let me exit from here and then copy to
clear the lab plus question.
Over here, everything is deleted.
I should not see anything here.
Yes, so this is the configmap.yaml.
I also need to delete here that I created
manually.
So, I believe that this type of question can
be asked in the question, especially in CKAD
exam.
This is very simple.
After completing the 4 labs, this question
is very simple.
It's a cup of tea for me now.
if you like this video, please like, share,
and subscribe to my channel so that you will
get free contents in future and thank you.